'use strict'

/** STATUS: FUTURE CRM DOMAIN — DISABLED BY DEFAULT — NOT PART OF V1 ROUTES. */

const { ClientScore, LeadTracker, NotesStore } = require('../core')
const { CRMService } = require('../services')

module.exports = { ClientScore, LeadTracker, NotesStore, CRMService }
