'use strict'

/** STATUS: FUTURE AUTOMATION DOMAIN — DISABLED BY DEFAULT — NOT PART OF V1 ROUTES. */

const { AutomationCenter, WebhookVerifier } = require('../core')
const { AutomationService } = require('../services')

module.exports = { AutomationCenter, WebhookVerifier, AutomationService }
