'use strict'

/** STATUS: FUTURE REPORTING DOMAIN — DISABLED BY DEFAULT — NOT PART OF V1 ROUTES. */

const { FounderReportService } = require('../services')

module.exports = { FounderReportService }
