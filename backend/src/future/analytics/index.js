'use strict'

/** STATUS: FUTURE ANALYTICS DOMAIN — DISABLED BY DEFAULT — NOT PART OF V1 ROUTES. */

const { AnalyticsEngine } = require('../core')
const { AnalyticsService, AnomalyDetector } = require('../services')

module.exports = { AnalyticsEngine, AnalyticsService, AnomalyDetector }
