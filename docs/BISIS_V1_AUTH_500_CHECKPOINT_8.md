Browser checkpoint — 2026-08-24

The visible navigation index changed between pages, and two attempts to select the Auth Observability item landed on the Edge Functions page. No mutation occurred. The next navigation will use the known project URL path `/observability/auth` directly, for read-only inspection.
