Browser checkpoint — 2026-08-24

The read-only query for the live definition of `public.handle_supabase_auth_user()` is visible in SQL Editor. The result panel still shows the previous auth.users trigger result, so the function-definition query has not yet been independently executed. No writes were performed.
